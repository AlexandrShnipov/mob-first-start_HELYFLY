import s from './Button.module.scss';

const Button = () => {

    return (

        <button className={s.btn} type='button'>
           Забронировать место
        </button>

    );
}

export default Button;